// All exercises' answers are saved in the DB, unless its type is in this array
const unsavedExerciseTypes = [
  'characterStroke',
  'textToSpeech',
  'audioToSpeech'
];

export default unsavedExerciseTypes;
