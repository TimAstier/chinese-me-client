{
  "name": "chinese-me-client-server",
  "version": "0.0.0",
  "description": "Simple express server for the ChineseMe client.",
  "private": true,
  "engines": {
    "node": "6.9.x"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd react-ui/ && npm install --only=dev && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "express": "^4.14.1",
    "prerender-node": "^3.1.1"
  }
}
